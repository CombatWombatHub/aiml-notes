[project]
name = "general-machine-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=6.30.0",
    "matplotlib>=3.10.3",
    "numpy>=2.3.2",
    "pandas>=2.3.1",
    "scikit-learn>=1.7.1",
    "scipy>=1.16.1",
    "seaborn>=0.13.2",
]

[dependency-groups]
app = [
    "streamlit>=1.47.1",
]
docs = [
    #"accessible-pygments==0.0.4", # GitHub Actions failed trying to install 0.0.5
    # great, when I do that it then fails at alabaster==1.0.0, latest it has is 0.7.13
    # it's probaby failing alphabetically and accessible-pygments was just the first one
    # is it the Python version of the GitHub Actions job?
    "myst-parser>=4.0.1",
    "nbsphinx>=0.9.6",
    "sphinx>=8.2.3",
    "sphinx-book-theme>=1.1.4",
]
